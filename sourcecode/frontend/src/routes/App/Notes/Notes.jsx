import React from 'react';
import { Editor } from '../../../components/Editor';

const Notes = () => {
    return (
        <div>
            <Editor />
        </div>
    );
};

export default Notes;
