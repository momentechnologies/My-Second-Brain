import React from 'react';
import { Box } from '@mui/material';

const Divider = () => {
    return (
        <Box
            sx={{
                width: '1px',
                backgroundColor: '#eee',
                margin: '0 4px',
            }}
        />
    );
};

export default Divider;
